# 错误代码对照（修改前 → 修改后）

与《问题记录.md》编号对应。仓库没有用 git，这里按当时的修改记录还原。

## 1. YAML 布尔陷阱（config/sort.yaml）
```yaml
# 修改前：y 被 YAML 1.1 解析成 True，节点报 "expecting type STRING"
jaw_axis: y
# 修改后
jaw_axis: 'y'
```

## 2. rclpy 日志级别（task_manager.py）
```python
# 修改前：同一行代码换级别调用 → ValueError('Logger severity cannot be changed between calls.')
getattr(self.get_logger(), level)(text)
# 修改后
if level == 'warn':
    self.get_logger().warn(text)
elif level == 'error':
    self.get_logger().error(text)
else:
    self.get_logger().info(text)
```

## 3. 翻腕解 + 指垫侧向偏置（kinematics.py）
```python
# 修改前：align_jaw 只把虎口转到最近的 90° 倍数，J6 可能停在 ±180° 附近（手腕整体翻转，压到邻格）
q[5] = np.clip(q[5] - delta / dphi, JOINT_LIMITS[5][0], JOINT_LIMITS[5][1])
return q
# 修改后：虎口对称，J6 归一到 (-90°, 90°]
while q[5] > math.pi / 2:
    q[5] -= math.pi
while q[5] <= -math.pi / 2:
    q[5] += math.pi
return q

# 修改前：指尖 = 夹爪基座原点沿接近轴前伸，忽略指垫 1 cm 侧向偏置
def tip_pos(q, tool_offset):
    T6, Tg = fk(q)
    return Tg[:3, 3] + T6[:3, 2] * tool_offset, T6[:3, 2]
# 修改后：加 gripper z 方向 -0.010（仿真真值实测）
TOOL_LATERAL = -0.010
def tip_pos(q, tool_offset, lateral=None):
    lat = TOOL_LATERAL if lateral is None else lateral
    T6, Tg = fk(q)
    return Tg[:3, 3] + Tg[:3, 1] * tool_offset + Tg[:3, 2] * lat, Tg[:3, 1]
```

## 6. 反手够的逆解（kinematics.py）
```python
# 修改前：收敛的解里只按"离参考构型近"挑，随机初值可能给出 J1 转 150° 绕到背后的解
solutions = [sol for sol in (run(q) for q in seeds[:n_analytic]) if sol is not None]
# 修改后：基座转角必须朝向目标方位
def facing(sol):
    d = (sol[0] - yaw + math.pi) % (2 * math.pi) - math.pi
    return abs(d) <= math.pi / 2
solutions = [sol for sol in (run(q) for q in seeds[:n_analytic]) if sol is not None and facing(sol)]
```

## 7. 横移路径下沉（planner.py）
```python
# 修改前：放完直接从放置点上方转回正前，放置点低时贴着桌面扫回来
back = wp['above_zone'].copy()
back[0] = 0.0
# 修改后：先抬回 carry（过渡高度、区域方位），再只转 J1
carry = wp['transit'].copy(); carry[0] = wp['above_zone'][0]
back = carry.copy(); back[0] = 0.0
```

## 13. 示教工具读角度（teach_points.py）
```python
# 修改前：臂内服务返回 {"ok": true, "angles": [...]}，把整包当列表 → could not convert string to float: 'ok'
angles = [float(a) for a in cli.get_angles()]
# 修改后
resp = cli.get_angles()
raw = resp.get('angles') if isinstance(resp, dict) else resp
angles = [float(a) for a in raw]
```

## 15. 示教超限不查（teach_points.py）
```python
# 修改前：读到什么存什么，J4=-174°、J5=156° 的翻腕姿态也存进去了
# 修改后：记录时查限位，超了要求重摆；记录后低速回放验证
LIMIT_MIN = [-160, -75, -175, -155, -115, -175]
LIMIT_MAX = [160, 120, 65, 155, 115, 175]
bad = limit_problems(angles)   # 返回超限关节列表，非空则提示重摆
...
r = cli.goto(ang, a.speed)      # 回放验证，残差 > 3° 判为走不到
```

## 16. 失败后原地扔方块（pick_place_server.py）
```python
# 修改前：带着方块回放置点上方失败就原地松爪
except StepFailed as e:
    note = f'；带物体回放置点上方失败（{e.message}），原地释放'
self.move_gripper(self.grip_open)
# 修改后：放置点上方 → 取物格过渡点 → 都不行才原地
for name, q in (('放置点上方', self.holding), ('取物格过渡点', self.holding_fallback)):
    try:
        self.move_arm(q, self.move_dur * 0.6); released = True; break
    except StepFailed: ...
```

## 17. 空占位落点（pick_place_server.py）
```python
# 修改前：区域落点列表 [点, 点, []]，第三个方块按计数取到空项 → "示教点需要 6 个关节角" → 判不可达
slots = zones.get(zone_id)
degs = slots[slot % len(slots)]
if len(degs) != 6:
    raise Unreachable(...)
# 修改后：只用真正教过的点轮流用
slots = [p for p in (zones.get(zone_id) or []) if p is not None and len(p) == 6]
degs = slots[slot % len(slots)]
```

## 18. 相机帧积压（usb_camera.py）
```python
# 修改前：定时器 15 Hz 调 read()，相机 30 fps 出帧，V4L2 队列里永远积着几帧 → 画面慢半拍
def _tick(self):
    ok, frame = self.cap.read()
# 修改后：单开线程不停 read() 把队列吃空，定时器只发最新一帧
def _capture_loop(self):
    while self.alive:
        ok, frame = self.cap.read()
        with self.lock:
            self.latest = frame
def _tick(self):
    with self.lock:
        frame, self.latest = self.latest, None
```

## 19. 方块顶面视差（grid_locator.py / task_manager.py）
```python
# 修改前：检测框中心直接乘单应矩阵，当成桌面上的点（相机低时偏 3-4 cm，格 3 报空）
def pixel_to_world(self, u, v):
    xy = self.pixel_to_world_batch([[u, v]])[0]
# 修改后：用单应矩阵反推相机位置 cam，按高度比例把点拉回朝相机方向
def pixel_to_world(self, u, v, height=0.0):
    xy = self.pixel_to_world_batch([[u, v]])[0]
    if height > 0 and self.cam is not None and self.cam[2] > height:
        xy = xy + (self.cam[:2] - xy) * (height / self.cam[2])
```

## 23. 被跳过的格永不再理（task_manager.py）
```python
# 修改前：skipped 只增不减，attempts 只累加；老师再往该格放方块也不处理
if cid in self.skipped:
    continue
# 修改后：格子变空即清除跳过标记和失败计数；类别变化也重新参与；全部处理完后对夹取失败格补抓一轮
for cid, (cls, score, n) in status.items():
    if cls == 'empty':
        self.skipped.pop(cid, None); self.attempts[cid] = 0
```

## 14. 不是代码，是操作失误
同步代码时 `rsync mecharm_sort` 把包内的示教模板 `config/taught_points.yaml` 盖掉了现场示教好的点。
处理：示教文件移到 `~/Desktop/exp3_taught_points.yaml`，rsync 加 `--exclude taught_points.yaml`。
