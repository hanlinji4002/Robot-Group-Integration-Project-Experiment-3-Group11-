# 实验三 问题记录（2026-09-12）

按时间顺序，每条：现象 → 原因 → 处理。仿真阶段 1–12，真机阶段 13–24。

## 一、仿真阶段

| # | 现象 | 原因 | 处理 |
|---|---|---|---|
| 1 | 抓取服务器启动即崩，报参数类型错误 | YAML 里 `jaw_axis: y` 被当成布尔值 True | 写成 `'y'` |
| 2 | 任务节点报 "Logger severity cannot be changed" | rclpy 同一行代码不能用不同日志级别 | info/warn/error 分开写 |
| 3 | 第一次抓取下降步超时，邻格方块被推走 | 逆解给出 J6≈180° 的翻腕解；夹爪指垫相对法兰有 1 cm 侧向偏置，翻腕后整个手腕压到邻格 | J6 归一到 ±90° 内；用仿真真值量出偏置并计入运动学模型 |
| 4 | Jetson 上每次规划 8–24 s | 纯 Python 数值雅可比逆解，15 个初值全跑 | 初值按顺序、收敛即停；启动时预算全部 6 格 + 落点并缓存（约 10 s） |
| 5 | 前方料盒第二个方块放不下去，轨迹超时 | 落点间距 2.5 cm 等于方块边长，贴着第一个被顶住 | 间距改 3.5 cm；放置时虎口对齐与排列垂直的轴 |
| 6 | 某些格逆解给出 J1 转 150° 绕到背后反手够的解 | 只按"离参考构型近"选解，没限制方位 | 硬过滤：基座转角必须朝向目标（|J1−yaw| ≤ 90°） |
| 7 | 网格到料盒的横移路径上夹爪最低点只有 7 mm | 六轴同时插值，半路下沉 | 加 carry/back 两个中间点：过渡高度上先只转 J1，再落到落点；回程同理 |
| 8 | 用户反馈：回零姿态不对、方块松爪前掉落、网格离底座太近 | 折叠扫描姿态与实验二/真机不一致；放置允许倾斜 40° 方块从指间滑出；网格设计太近 | 回零改全零 L 形；搬运/放置全部竖直，释放高度 3 cm→8 mm；网格后移。正上方相机会被小臂挡住，相机改装侧上方斜视 |
| 9 | 用户圈定的网格位置 x≈0.19–0.28 m | 超出 mechArm 270 竖直抓取极限（≈0.19–0.20 m） | 网格改连片 3×2（格 5×6 cm）推到可达前沿 x=0.105/0.155；前方区放 0.20 m，只留 2 个落点 |
| 10 | 用户要求去掉料盒 | — | 分类区改为网格左/前/右的空桌面矩形（zones） |
| 11 | 真机相机位置不固定，仿真"标定相机位姿"路线不成立 | — | 网格纸四角印 4 个 ArUco 码，每次扫描自动解像素→桌面单应矩阵；仿真同款验证（相机挪到右侧照常 6/6） |
| 12 | 仿真里远处两个码检不到 | 码只有 24 px 宽，ArUco 默认参数漏掉 | 放宽检测参数，码放大到 3.5 cm，仿真相机改 1280×720 与真机一致 |

## 二、真机阶段

| # | 现象 | 原因 | 处理 |
|---|---|---|---|
| 13 | 示教工具崩溃 `could not convert string to float: 'ok'`，臂留在松力矩状态 | 臂内服务返回整包 JSON，角度在 `angles` 字段 | 按字段取值；新增 `teach_points hold/soft/angles` 恢复命令 |
| 14 | 示教好的点丢失（两次） | 同步代码时 rsync 把包里的 taught_points.yaml 模板盖掉了 | 示教文件移到工作区外 `~/Desktop/exp3_taught_points.yaml`，rsync 排除；第一批数值从聊天记录恢复成备份 |
| 15 | 第一批示教点 J4≈±175°、J5≈150–173° | 变软手摆时手腕翻转过去，超出 J4 ±155 / J5 ±115 限位，回放会被拒绝 | 记录时查限位并提示重摆；记录后自动低速回放验证 |
| 16 | 绿色→左区放不下，到位残差几十度，方块原地释放 | 左区落点用 `--presim` 教出 J3=45/J4=−56/J5=89 的怪姿态，固件拒绝执行（真机 J5 方向与仿真不一致，预摆位把手腕带歪） | 重教该点，不再推荐 `--presim`；失败兜底改为退回取物格过渡点再松爪，不再原地扔 |
| 17 | 格 3、4、6 报"不可达"跳过 | 区域落点列表里有早先留下的空占位 `[]`，第三个同色方块按计数取到了空项 | 只用真正教过的点轮流使用，空项忽略 |
| 18 | 相机画面很卡 | 相机 30 fps 出帧、节点 15 Hz 读，V4L2 队列积压；1280×720 原图发给多个订阅者 | 单线程不停读只留最新帧；调试图缩半、隔帧发；YOLO 开 FP16。TensorRT 引擎转好但无提升（模型 3M 参数，本来 21 ms/帧） |
| 19 | 格 3 明明有方块却报空 | 单应矩阵只对桌面点准；相机低（离桌 27 cm）斜视，2.5 cm 高的方块顶面投影到桌面外 3–4 cm | 用单应矩阵反推相机位置，把检测点沿朝相机方向拉回（视差修正）；容差 2.5→3 cm |
| 20 | 回零报 "No route to host" | 臂内树莓派正在重启，还没上线 | 等它起来后远程起 arm_server；记录一条远程启动命令 |
| 21 | 抓起后举太高，连跑后夹爪发热、抓力下降掉方块 | 抬升 J2 回收 25°/35°（8–10 cm） | 改 12°/18°（4 cm / 5–6 cm），做成 launch 参数 |
| 22 | 动作太慢（驱动算出来 5%） | move_duration 4 s、max_speed 20 | 2 s / 40%，做成 launch 参数 |
| 23 | 中途补的方块、失败过的格不再处理 | 被跳过的格永久跳过，失败计数不清零 | 格子变空/类别变化即重新参与；1→6 处理完后对夹取失败格补抓一轮；循环上限 12→30 |
| 24 | 用户要求简化放置 | — | 每种颜色 1 个空中放置点，到点直接松爪（drop_in_air） |

## 三、验收时值得写进报告的点

- 异常处理实际覆盖：空网格、未识别物体（黄色方块）、目标不可达（未示教/超限）、夹取失败重试与补抓、找不到网格定位码、通信故障安全停止。
- 仿真→真机只换了外设与参数：相机节点、识别节点、驱动、示教回放、ArUco 定位；状态机与抓取动作接口未改。
